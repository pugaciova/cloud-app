apiVersion: v1
kind: Service
metadata:
  name: cloud-app-service
  namespace: cloud-app
  labels:
    app: cloud-app
spec:
  type: LoadBalancer
  selector:
    app: cloud-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
