apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cloud-app-monitor
  namespace: cloud-app
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: cloud-app
  namespaceSelector:
    matchNames:
      - cloud-app
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
